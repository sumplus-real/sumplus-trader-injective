# Runtime deps for the deployed container (live loop + dashboard) ONLY.
# Excludes web3 — it is used solely by the one-time identity/commit scripts (run by hand
# before the window), never by the loop or the dashboard, so it stays out of the image.
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
pydantic>=2.0
python-dotenv>=1.0
